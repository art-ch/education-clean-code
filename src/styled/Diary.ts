import styled from 'styled-components';

const DiaryWrapper = styled.section`
  .diary-card {
    background: #ffaa00;
  }
  .container {
    margin: 2rem auto;
  }
  .quiz-card {
    width: 300px;
    min-height: 250px;
  }
`;

export default DiaryWrapper;
