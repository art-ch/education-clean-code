import styled from 'styled-components';

const TrainScheduleWrapper = styled.section`
  .train-schedule-card {
    background: #2b9348;
    .card {
      margin: 2rem 0;
    }
  }
`;

export default TrainScheduleWrapper;
